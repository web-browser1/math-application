# Server port for this microservice
server.port=8083

# Service name for identification (optional)
spring.application.name=trigonometric-microservice

# Disable security for simplicity (remove in production)
spring.security.basic.enabled=false

# Optional: Actuator for monitoring the service
management.endpoints.web.exposure.include=*